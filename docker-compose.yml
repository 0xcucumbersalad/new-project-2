services:
  bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
